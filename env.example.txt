# Copy this file to .env and update with your values

# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/trineo-tasks
# For production, use MongoDB Atlas or similar:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/trineo-tasks?retryWrites=true&w=majority

# JWT Secret (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
# Generate a secure secret: openssl rand -base64 32

# CORS Configuration (comma-separated list of allowed origins)
# Leave empty or set to true to allow all origins (development only)
# For production, specify your domain:
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Frontend API URL (for Vite proxy in development)
VITE_API_URL=http://localhost:5000

